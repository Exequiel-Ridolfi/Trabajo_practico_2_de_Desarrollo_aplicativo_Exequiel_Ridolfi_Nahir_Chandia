import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Trabajo_Practico_2_D_A_Exequiel_Ridolfi_Nahir_Chandia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
